<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.WiFiManager" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/item_primary_dark</item>
        <item name="colorPrimaryVariant">@color/item_primary_variant_dark</item>
        <item name="colorOnPrimary">@color/item_on_primary_dark</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/item_secondary_dark</item>
        <item name="colorOnSecondary">@color/item_on_secondary_dark</item>

        <!-- Background color. -->
        <item name="android:colorBackground">@color/background_dark</item>

        <!-- Surface color. -->
        <item name="colorSurface">@color/surface_dark</item>
        <item name="colorOnSurface">@color/on_surface_dark</item>

        <!-- System bar colors. -->
        <item name="android:statusBarColor">?attr/colorOnSurface</item>
        <item name="android:navigationBarColor">?attr/colorOnSurface</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>
</resources>